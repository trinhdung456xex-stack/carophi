-- // Robeowemeantay Hub
-- // Made for your own game

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Robeowemeantay",
    LoadingTitle = "Robeowemeantay Hub",
    LoadingSubtitle = "by your name",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "Robeowemeantay_Config",
        FileName = "Robeowemeantay_Settings"
    },
    Discord = {
        Enabled = false,
        Invite = "noinvite"
    },
    KeySystem = true,
    KeySettings = {
        Title = "Robeowemeantay | Key System",
        Subtitle = "Key Required",
        Note = "Key: robeo",
        FileName = "RobeowemeantayKey",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"robeo"}  -- Key chính thức
    }
})

-- Tab chính
local MainTab = Window:CreateTab("Main", 4483362458)
local MiscTab = Window:CreateTab("Misc", 4483362458)

-- Notification khi load xong
Rayfield:Notify({
    Title = "Robeowemeantay",
    Content = "Đã load thành công! Chúc bạn chơi vui :)",
    Duration = 5,
    Image = 4483362458,
})

-- ==================== MAIN TAB ====================
MainTab:CreateSection("Auto Farm")

MainTab:CreateToggle({
    Name = "Auto Farm Level",
    CurrentValue = false,
    Flag = "AutoFarm",
    Callback = function(Value)
        print("Auto Farm:", Value)
        -- Thêm code farm của bạn ở đây
    end,
})

MainTab:CreateToggle({
    Name = "Auto Quest",
    CurrentValue = false,
    Flag = "AutoQuest",
    Callback = function(Value)
        print("Auto Quest:", Value)
    end,
})

MainTab:CreateSlider({
    Name = "Farm Distance",
    Range = {10, 200},
    Increment = 10,
    CurrentValue = 50,
    Flag = "FarmDistance",
    Callback = function(Value)
        print("Distance set to:", Value)
    end,
})

-- ==================== MISC TAB ====================
MiscTab:CreateSection("Misc Features")

MiscTab:CreateButton({
    Name = "Teleport to Safe Zone",
    Callback = function()
        print("Teleported to safe zone")
        -- Thêm code teleport
    end,
})

MiscTab:CreateToggle({
    Name = "ESP Players",
    CurrentValue = false,
    Flag = "ESP",
    Callback = function(Value)
        print("ESP:", Value)
    end,
})

MiscTab:CreateInput({
    Name = "Set WalkSpeed",
    PlaceholderText = "Nhập số...",
    RemoveTextOnFocus = false,
    Callback = function(Text)
        print("WalkSpeed set to:", Text)
    end,
})

print("Robeowemeantay Hub loaded successfully!")
